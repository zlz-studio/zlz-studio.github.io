name: Staging Build and Deploy

on:
  push:
    branches: [staging]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'

      - name: Install dependencies
        run: gem install jekyll kramdown kramdown-parser-gfm

      - name: Auto-update last_modified_at from git log
        run: |
          python3 - <<'EOF'
          import subprocess, re, os

          for root, dirs, files in os.walk('.'):
              dirs[:] = [d for d in dirs if d not in ['.git', '_site', '_includes', '_layouts']]
              for fname in files:
                  if not fname.endswith('.md'):
                      continue
                  path = os.path.join(root, fname)
                  result = subprocess.run(
                      ['git', 'log', '-1', '--format=%Y-%m-%d', '--', path],
                      capture_output=True, text=True
                  )
                  date = result.stdout.strip()
                  if not date:
                      continue
                  with open(path, 'r', encoding='utf-8') as f:
                      content = f.read()
                  content = content.replace('\r\n', '\n')
                  new_content = re.sub(
                      r'^last_modified_at:.*$',
                      f'last_modified_at: {date}',
                      content,
                      count=1,
                      flags=re.MULTILINE
                  )
                  if new_content != content:
                      with open(path, 'w', encoding='utf-8') as f:
                          f.write(new_content)
                      print(f'Updated: {path} -> {date}')
          EOF

      - name: Debug - check front matter after script
        run: |
          echo "=== features/Outline/index.md first 6 lines ==="
          head -6 features/Outline/index.md | cat -A
          echo "=== setup-character/standard-setup/index.md first 6 lines ==="
          head -6 setup-character/standard-setup/index.md | cat -A

      - name: Build Jekyll (staging)
        run: jekyll clean && jekyll build

      - name: Deploy to gh-pages/staging
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
          destination_dir: staging
          keep_files: true
